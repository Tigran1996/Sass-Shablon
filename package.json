{
  "name": "scss",
  "version": "1.0.0",
  "description": "",
  "main": "/index.js",
  "scripts": {
    "compile:sass": "node-sass src/sass/main.scss src/css/style.css -w",
    "live-server": "live-server --port=2020 --open=src",
    "work": "concurrently --kill-others \"npm run compile:sass\" \"npm run live-server\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.2",
    "live-server": "^1.2.1",
    "node-sass": "^4.11.0"
  }
}
